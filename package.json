{
  "name": "pdf-api",
  "version": "1.0.0",
  "description": "API for generating PDFs from HTML using Puppeteer.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"No tests specified\""
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "puppeteer": "^21.5.0"
  }
}
