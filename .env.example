# PDF Generation API Environment Variables

# API Authentication Key (required for protected endpoints)
# Generate a secure random string for production use
API_KEY=your-secure-api-key-here

# Server Configuration
PORT=3000
NODE_ENV=development

# Security Settings
HTTPS_REDIRECT=false

# Example of a secure API key (replace with your own):
# API_KEY=pdf_api_2024_secure_key_abc123xyz789

# Instructions:
# 1. Copy this file to .env
# 2. Replace 'your-secure-api-key-here' with a strong, unique API key
# 3. Never commit the .env file to version control
# 4. Use different keys for development, staging, and production environments